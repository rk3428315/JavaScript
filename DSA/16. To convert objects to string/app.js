  let student = {
   
    first : "Ravi",
    last  : "Kumar",    
    age     : 22,
    address : "Alipur Mandra"
  }
    let strr = Object.keys(student) +"-"+ Object.values(student) 
 // let strr = student.name.toString() + student.age.toString() + student.address.toString();
  console.log(typeof strr, strr);